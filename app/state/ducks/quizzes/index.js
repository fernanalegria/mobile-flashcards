import reducer from './reducers';
import * as quizActions from './actions';
import * as quizTypes from './types';
import quizShape from './models';

export { quizActions, quizTypes, quizShape };
export default reducer;
